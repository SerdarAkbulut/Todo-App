
Bu proje, .NET 8 kullanýlarak geliþtirilmiþ basit ama geniþletilebilir bir **Todo Yönetim API’si**dir.  
Kullanýcýlarýn kimlik doðrulama ile giriþ yapabileceði, görev ekleme, güncelleme, silme ve listeleme iþlemlerini yapabileceði bir yapýya sahiptir.

---

## ?? Özellikler

- ? Kullanýcý kayýt ve giriþ sistemi (Identity)
- ? JWT (JSON Web Token) ile kimlik doðrulama
- ? Todo ekleme, listeleme, güncelleme ve silme
- ? Entity Framework Core ile veritabaný yönetimi
- ? DTO (Data Transfer Object) katmaný kullanýmý
- ? Katmanlý mimari (Entity, Data, Business, API)
- ? Swagger UI ile API test imkaný